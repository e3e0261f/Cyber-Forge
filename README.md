# ⚔️ 《天道锻造大师 WEB版》 (Cyber-Forge)

> **当前版本**：`v2.5.1`  
> **版本代号**：`WEEB` (Web-Enabled Exponential Engine Base)  
> **核心架构**：Rust (Actix-web) 高并发微服务 + HTML5/WebGL (Pixi.js v8) 纯显卡渲染引擎  
> **设计哲学**：**工业级高性能标准（零 DOM 污染、全屏 WebGL 硬件加速、客户端预测 0ms 打击感、无上限阶梯数值）**

## 🆕 增量更新：任务系统与交互增强

> 本节为最近一次增量更新说明，原有技术白皮书与开发路线保持不变。

### 更新内容

- **任务系统**：随机生成 3～8 个候选任务，最多同时接取 5 个；支持押镖、跑商、击杀目标、物品提交。
- **保证金**：接取时立即扣除任务指定的金币或仙玉；放弃任务损失全额保证金；完成并领取奖励时返还完整保证金。价值换算参考为 `1 仙玉 = 10,000 金币`，不会擅自替玩家兑换资产。
- **离线倒计时**：押镖、跑商、击杀任务使用 Unix 时间戳，关闭页面后再次进入仍按真实时间计算；倒计时结束必定完成，不使用成功率判定。
- **手动物品提交**：玩家必须在任务窗口选择背包物品，后端会复核物品 ID、非工具属性和最低品质，提交成功后消耗物品。
- **奖励保护**：奖励支持金币、仙玉和神兵；背包满时神兵奖励保留在待领取任务中，不会丢失。
- **任务入口**：新增 `📋 任务(J)`，位置为 `拍阁(P) → 任务(J) → 学徒(M)`，快捷键为 `J`。
- **背包排序**：自动排序默认关闭，点击排序按钮后循环 `关闭 → 默认 → 品质 → 价格 → 时间 → 关闭`。
- **滚动条**：背包、拍卖行、日志页支持滚轮、轨道点击定位和滑块拖拽；任务窗口支持滚轮浏览。

### 任务接口

统一使用 `POST /api/action`，请求体格式：

```json
{ "key": "quest_accept_<任务ID>" }
```

| 动作 key | 用途 |
| --- | --- |
| `quest_accept_<id>` | 接取候选任务并扣保证金 |
| `quest_abandon_<id>` | 放弃任务，保证金不返还 |
| `quest_submit_<quest_id>_<item_id>` | 提交指定背包物品 |
| `quest_claim_<id>` | 领取奖励并返还保证金 |

任务数据随 `GET /api/state`、`POST /api/tick` 和动作响应返回：

```json
{
  "quests": [],
  "active_quests": [],
  "quest_next_refresh_secs": 300
}
```

候选任务字段包括 `id`、`kind`、`title`、`description`、`advanced`、`duration_secs`、`deposit`、`currency`、`reward`、`required_rank`；进行中任务还包括 `accepted_at`、`complete_at`、`completed`、`claimed`、`submitted_item_id`。

### 现有 API 快速使用

```text
GET  /api/state   获取完整游戏快照
POST /api/strike  手动挥锤并返回快照
POST /api/tick    推进心跳、任务倒计时、市场和自动化逻辑
POST /api/action  执行升级、货币、背包、拍卖和任务动作
```

示例：

```sh
curl http://127.0.0.1:8080/api/state
curl -X POST http://127.0.0.1:8080/api/tick
curl -X POST http://127.0.0.1:8080/api/action \
  -H 'Content-Type: application/json' \
  -d '{"key":"quest_accept_<任务ID>"}'
```

### 任务模块位置

- `src/game/quests.rs`：任务模型、刷新、保证金、倒计时、提交、放弃、领奖。
- `src/game/state/mod.rs`：`GameState.quests` 存档字段。
- `src/main.rs`：任务快照、`/api/tick` 推进和 `/api/action` 路由。
- `ui/js/quest-view.js`：任务窗口、任务卡片和手动物品选择。
- `ui/js/config.js`、`ui/js/input.js`、`ui/js/hud.js`：任务按钮、J 快捷键、交互与绘制接入。

### 验证命令

```sh
cargo fmt --all
cargo check
cargo test
git diff --check
```

---

---

## 🌟 架构演进与全景概览

本项目经历了从 **桌面端混合架构 (Tauri + DOM)** 到 **工业级纯 Web 架构 (Actix-web + WebGL Canvas)** 的彻底蜕变：
* **彻底脱离桌面壳子**：无需安装客户端，直接在任意现代浏览器中以 **60~144 FPS** 满血运行。
* **前后端极致解耦**：
  * **后端 (Rust / Actix-web)**：充当“天道总账房”，负责权威数值计算、高并发异步状态机、防作弊校验与数据持久化。
  * **前端 (Pixi.js / Canvas 2D)**：充当“全息大世界与操作终端”，拥有独立的本地高刷物理时钟与 0ms 实时 QTE 判定，彻底消除网络抖动带来的读条回跳与抽搐。

---

## ✅ 已完成功能清单 (Completed Features)

### 1. 🚀 后端与通信架构 (Backend & Network Engine)
- [x] **Actix-web 纯 Web 服务**：提供 `/api/state`、`/api/strike`、`/api/tick`、`/api/action` 四大高速 API，并内嵌托管静态前端资产。
- [x] **全按键 $O(1)$ 批量瞬时计算**：重构了所有升级与货币兑换逻辑，彻底消除慢速循环与请求超时，支持单次千万级批处理秒级入账。
- [x] **阶梯渡劫与残酷惩罚机制**：
  - 10层 (75%) $\to$ 11层 (80%) $\to$ 12层 (90%) $\to$ 13层 (91%) $\to$ 之后每层 $+1\%$（最高封顶 99%）。
  - 渡劫失败触发天劫反噬：真元溃散，修为尽失，贬回炼体境一重。
- [x] **大境界突破战力巅峰记忆**：重构 `total_level` 计算，引入 `max_total_level` 机制，确保大境界突破后战力与增益只涨不跌。
- [x] **神兵 64-bit 天道四维指纹位域打包** (`Fingerprint64`)：
  - 将 `24-bit 时辰天干` + `11-bit 地轴太极` + `12-bit 道德经印记` + `17-bit 始祖哈希` 压缩入单个 `u64`。
  - 通过 Base62 无损转码生成 8~10 位天道铭文短码（如 `#Z7kQ-9mA3F2`）。

---

### 2. 🎨 2D 赛博修真大世界 (2D Canvas / WebGL Visuals)
- [x] **全屏单一画布渲染**：彻底清除 HTML 布局标签，全屏 100vw $\times$ 100vh 由 GPU 直接绘制，零 DOM 布局开销。
- [x] **太古灵石神坛全景原画背景**：采用无损 Aspect-Ratio Cover 算法铺满，彻底消除拉伸畸变。
- [x] **黑金天道铁砧绝对锚定**：铁砧死死吸附在背景中央八卦石台的同心圆中心，随窗口缩放永不脱节。
- [x] **铁砧参数化自由微调面板** (`ANVIL_CONFIG`)：在代码顶部直接调节铁砧与圣剑的缩放、旋转角度与像素偏移。
- [x] **真实 2D 原画资产全盘接入**：
  - `workshop_bg.webp`（太古神坛全景）
  - `anvil.png`（2.5D 黑金天道巨砧）
  - `sword_base.png`（玄铁飞剑底胎）
  - `piston_arm.png`（黄铜机械动力风箱）
  - `apprentice_robot.png`（发条小机偶小天机）
  - `crystals.png`（通体透亮幽蓝灵晶簇）
- [x] **0 延迟挥锤打击感系统**：
  - 0ms 本地 QTE 暴击判定（0.76 ~ 0.88 区间即时响应）。
  - 0ms 读条归零重蓄与漫天火花物理喷射。
  - 浮空伤害数字跳动（`+13.97 淬火` / `⚡ 完美暴击`）。
  - 击锤瞬间全场景环境漫反射强光与屏幕微震。
- [x] **纯本地 144Hz 物理时钟**：彻底摆脱 150ms 网络轮询脉冲的拖累，进度条与 35 根矩阵小条丝滑流动，0 抽搐、0 回跳。

---

### 3. 🖥️ 路线 A 全息赛博弹窗与交互 (Interactive Modals)
- [x] **顶部 HUD 看板与导航**：
  - 实时资产看板（铜钱、金币、仙玉）与自动单位缩写（K, M, G, T, P, E, Z, Y）。
  - 快捷导航按钮：`[🎒 锦囊(B)]`、`[🏛️ 拍阁(P)]`、`[🛠️ 学徒(M)]`、`[📜 日志(I)]`、`[👤 身体(C)]`。
- [x] **多窗口并存与拖拽记忆**：
  - 支持最多 **3 个窗口同时打开并排展示**，互不遮挡。
  * 窗口外**无任何暗黑/模糊遮罩**，背景工坊打铁 100% 保持可见与高刷运转。
  - 鼠标按住窗口标题栏可自由拖拽，**且下次打开自动记住上次拖放的位置**。
  - 边框极光扫光光束 (Sweeping Laser) 与四角机械定位角标 `[ ┘ └ ]`。
- [x] **矩阵锦囊 (背包) 深度重构**：
  - 10 列紧凑型网格，单格 38px，超大巨幕视野。
  - **鼠标滚轮平滑滚动**，支持 700+ 格子全量浏览。
  - **物品鼠标自由拖拽换位 (Drag & Drop)**。
  - **停用后端自动强制重排**，增加右上角手动排序循环按钮 `[ 排序: 默认/品质/价格/时间 ▾ ]`。
- [x] **自动货币协议内置于背包页**：
  - 移出主 HUD，收纳于背包右上角。
  - 支持 `[天道纳玉]`（自动存玉）、`[灵虚兜底]`（自动碎玉买东西）、`[协议关闭]`（纯手动）三态切换。
- [x] **藏宝阁拍卖大厅全息弹窗**：600+ 件拍品全量支持滚轮平滑滚动与实时竞价倒计时。
- [x] **学徒工坊全息弹窗**：五大岗位（磨剑、附魔、熔铸、盲锻、拍卖）独立卡片与一键调配。
- [x] **全息神兵出生证明卡片**：点击全息蓝图或在背包悬停神兵，弹出完整的四维天道出生证明 Tooltip。
- [x] **右键赛博快捷菜单**：拦截浏览器默认菜单，弹出自定义快捷操作面板。

---

### 4. ⌨️ 全键盘输入控制 (Input Engine)
- [x] **空格键疯狂扫射**：默认 35ms 连击频率（每秒约 28 锤），按住即触发无限连锤。
- [x] **非空格键无上限指数级阶梯加速**：
  - 基础间隔 250ms。
  - 连续按住每 10 点，步进倍率自动 $\times 10$（$+1 \to +10 \to +100 \to +1000 \dots \to +10^{15}$），上不封顶！
- [x] **MMO 独立开关键位**：`C` 角色、`B` 锦囊、`P` 拍阁、`M` 学徒、`I` 日志、`ESC` 一键关闭所有弹窗。
- [x] **系统快捷键放行**：彻底解决 `Ctrl+Shift+R`、`F5`、`F12` 被拦截的问题；拦截 `Ctrl+滚轮` 页面缩放。
- [x] **独立系统配置文件 (`ui/js/config.js`)**：所有按键频率、阶梯阈值与导航定义全部参数化置顶。

---

## 📌 未完成与未来开发路线图 (TODO List)

### 阶段一：神兵四维生态深化与真实流转 (Sprint 2 残余)
- [ ] **神兵全息详情交互打磨**：
  - 彻底打通背包与拍卖行中每把具体神兵的真实反解字段（出生时辰、地轴经纬、始祖道号）。
  - 实现拍场物品的**历史竞拍落槌流转图谱**（记录历代主人道号与买入溢价）。

---

### 阶段二：密码学账号与天道安全体系 (Sprint 3)
- [ ] **《天道四言真经》助记词账号系统**：
  - 抛弃冷冰冰的随机单字，生成格式：`「甲辰子时 · 离火之极 · 剑宗逍遥 · 道法自然」`。
  - 前端确定性私钥种子推导，支持跨设备 1 秒“诵经还魂，无损漫游”。
- [ ] **非对称签名与信封加密体系 (KEK / DEK)**：
  - 服务器持有私钥对重要突破进行 Ed25519 签名；前端持有公钥验证，防本地内存修改。
- [ ] **Actix-web 令牌桶限流与 Hashcash 反向算力反制**：
  - 挂载 `governor` 中间件限制单 IP 请求频率。
  - 针对异常刷包客户端强制下发微型 PoW 谜题，反制恶意脚本。

---

### 阶段三：凡人实业与现代赛博金融工程 (Sprint 4)
- [ ] **宗门外包代工工单 (B2B Manufacturing Orders)**：
  - 前期稳定赚钱手段，定期刷新宗门批量农具/军械采购订单，零 RNG 积累第一桶金。
- [ ] **五行大宗原料跨域跑商与空间套利**：
  - 结合市井杂闻，低价买入溢出物料，跨区域运往短缺地界高价售出。
- [ ] **五行大宗原料期货合约 (Commodity Futures)**：
  - 允许玩家对庚金、丙火建立做多/做空仓位，利用杠杆进行大宗商品套保与投机。
- [ ] **蓬莱离岸群岛避税与天劫隔离信托**：
  - 设立离岸商会规避拍卖交易税。
  - 设立不可撤销家族信托，渡劫失败贬回凡人时，信托资产免受清算，支持快速重聚金身。
- [ ] **神兵质押借贷与资产证券化 (ABS)**：
  - 质押极品神兵获取 80% 现金流借贷。

---

### 阶段四：天道法则、全服事件与量子双修 (Sprint 5)
- [ ] **全服【天道熵池】状态机**：
  - 宏观统计全服打铁、消费、炸炉产生的熵增，影响全服熔炉温度与灵气潮汐。
- [ ] **全服大天劫 (Server Tribulation) & 世界 Raid 事件**：
  - 熵值达 100% 时全服警报，道友合力挥锤共铸【天道救世圣胚】，平息后全服拍卖天道神兵。
- [ ] **量子纠缠双修 (EPR Pair)**：
  - 两位道友绑定公钥纠缠对。
  - A 暴击时 B 屏幕触发量子光环并获得免费暴击充能；产出双生神兵共享拍卖分润。
- [ ] **天道合道者与诸天反抗军 (股东即天道)**：
  - 终局玩家身合天道成为全服股东，制定全服抽成比例；
  - 玩家积怨爆发时可触发【逆天伐道】战役，攻打天道王座。

---

### 阶段五：2D 打怪大世界与地图引擎融合 (Sprint 6)
- [ ] **三位一体地图寻路引擎集成**：
  - **Rot.js**：负责程序化生成无限随机的修仙地下城与洞府迷宫。
  - **NavMesh**：负责在 2.5D 斜 45° 地面上划定平滑多边形寻路网格。
  - **EasyStar.js**：负责玩家点击地图时毫秒级计算 A* 最佳走位路线。
- [ ] **玩家 2D 角色出门打怪与战斗视口**：
  - 玩家角色走出工坊大门，镜头平滑拉远，进入野外 2D 俯视角地图进行刷怪、走位、弹幕战斗与掉宝。
- [ ] **@pixi/ui 与 @pixi/sound 深度集成**：
  - 引入 PixiSound WebAudio 高性能音频引擎，配置金铁交鸣声、暴击音效与环境熔岩轰鸣。

---

### 阶段六：量子叠加态装备 (v2.5.2 规划)
- [ ] **未观测的混沌剑胚**：出炉武器初始为 `[ ??? · 叠加态剑胚 ]`。
- [ ] **神识观测 / 波函数坍缩开光**：点击时播放粒子聚变坍缩特效，亲手坍缩出绝世神兵。

---

## 🏗️ 目录结构规范 (Current Structure)

```text
Cyber-Forge-2.5/
├── Cargo.toml            # Rust 依赖声明 (Actix-web, Serde, Rand, Sha2)
├── Cargo.lock
├── README.md             # 本技术白皮书
├── src/
│   ├── main.rs           # Actix-web 服务器、API 路由与游戏心跳
│   └── game/             # 纯 Rust 游戏核心逻辑 (数值、境界、锻造、指纹、拍场)
│       ├── dao_origin.rs
│       ├── fingerprint.rs # 64-bit 四维指纹压缩引擎
│       ├── market_swarm.rs
│       ├── mod.rs
│       ├── numbers.rs
│       ├── realm.rs
│       ├── state/
│       │   ├── actions.rs
│       │   ├── encounters_lore.rs
│       │   ├── encounters.rs
│       │   ├── market.rs
│       │   ├── mod.rs
│       │   ├── save.rs
│       │   └── upgrades.rs
│       ├── strike.rs
│       ├── sword_gen.rs
│       ├── titles.rs
│       └── types.rs
└── ui/
    ├── assets/           # 2D 高清原画切片素材库 (WebP / PNG)
    ├── index.html        # 纯净单一全屏 Canvas 入口
    ├── styles.css        # 全屏画布无边距重置
    └── js/
        ├── app.js        # 游戏引擎总调度器 (极简引导)
        ├── core.js       # Web API 通信 (invoke) 与通用工具
        ├── config.js     # 🌟 全局系统参数与按键配置中心
        ├── state.js      # 全局状态机、物理时钟与多窗口持久化
        ├── hud.js        # 顶部看板、底部操作栏、右键菜单与全息视窗框架
        ├── input.js      # 交互总控 (MMO 快捷键、全键盘阶梯、鼠标拖拽)
        ├── stash-view.js # 矩阵锦囊背包 (10列紧凑、拖拽换位、滚轮浏览、手动排序)
        ├── auction-view.js # 藏宝阁拍卖大厅 (600+拍品全量滚轮浏览)
        ├── apprentice-view.js # 铁匠铺学徒工坊 (5大岗位调度)
        └── world/        # 2D 赛博工坊大世界模块
            ├── assets.js      # 素材异步预加载器
            ├── environment.js # 背景 Cover、透视网格
            ├── fx.js          # 粒子、地脉涟漪、浮空飘字
            ├── workshop.js    # 黑铁巨砧、圣剑、重锤、发条机偶、全息蓝图
            └── world.js       # 工坊总装配器
